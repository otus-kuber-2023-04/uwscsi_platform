# uwscsi_platform
uwscsi Platform repository
- Подготовка окружения
    - Установлен Docker Engine
    - Установлен Minikube
    - Установлен Cubectl
    - Установлен K9s, по каким-то причинам - отказался ставиться из магазина snap, установлен бинарник
    - Проверены контейнеры Minikube

- Выяснить причину автоматического восстановления контейнеров Minikube после их удаления
    - Начал с гугления, был найден ответ, но честно говоря сам до причин бы не докопался:
    - Все pods в namespace kube-system кроме сoredns востановились потому что их пересоздал 
	kubelet systemd демон работающий на самом хосте. 
	А pod coredns перезапустился потому что есть deployment с параметром replicas 1, 
	то есть, существует replicaset для coredns который его пересоздает если его нет. 
	Для kube-proxy есть daemonset который запускает его в одном экземпляре на каждой ноде.

- Создать Docker c простейшим вёб-сервером, отправить в Docker Hub, добавить init контейнер с инициализацией index.html
    - Создан Docker контейнер, отправлен в Docker Hub, контейнер корректно выдаёт файл hw.html
    - Создан Yaml файл с init контейнером
    - Запущен Web-Pod
    - Выполнен проброс портов в систему.
    - Проверено выполнение инициализации index.html, по адресу http://Localhost:8000 отображается страничка с ACSII псевдографикой

- Hipster Shop
    - Склонирован репозиторий
    - Сгенерирован контейнер, контейнер отправлен в Docker Hub
    - Под, запущенный в режиме Ad-Hoc запускается в статусе Error

- *
    - По логам - видно, что не инициализированы переменные
    - Создан Yaml-файл, куда перенёс инициализацию переменных из Yaml-файла по ссылке в описании ДЗ
    - Запущен под
    - Под выполняется
    - Скорректированный Yaml-файл добавлен в репозиторий.
